import Checkout from "@/features/shopkeeper/checkout/component/Checkout";

export const metadata = {
  title: "Checkout | Shopkeeper Dashboard",
  description: "POS Shopkeeper checkout and order processing system",
};

export default function CheckoutPage() {
  return <Checkout />;
}
