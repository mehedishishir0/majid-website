import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "img.freepik.com",
      "res.cloudinary.com",
      "api.dicebear.com",
      "i.pravatar.cc",
      "images.unsplash.com",
    ],
  },
};

export default nextConfig;
